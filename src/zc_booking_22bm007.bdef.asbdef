projection;
strict ( 2 );
use draft; // Must match the base BDEF

define behavior for ZC_BOOKING_22BM007 alias Booking
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;

  use action assignDriver;
  use action startTrip;
  use action completeTrip;
}